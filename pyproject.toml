[project]
name = "noaa-coops-async"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sean-Michael", email = "seanm.riesterer@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
noaa-coops-async = "noaa_coops_async:main"

[build-system]
requires = ["uv_build>=0.11.2,<0.12.0"]
build-backend = "uv_build"
